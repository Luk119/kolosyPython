from tkinter import *
import pandas as pd
import math

root = Tk()
root.title("logowanie")
root.geometry("400x500")

tekst1 = Label(root, text="Login:")
tekst1.grid(row=0, column=0)

tekst2 = Label(root, text="Password:")
tekst1.grid(row=0, column=0)

root.mainloop()

